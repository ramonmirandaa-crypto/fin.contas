# Prisma database connection URL (PostgreSQL 15)
# Exemplo para Dockploy/local: utilize o hostname do serviço Postgres configurado no cluster.
DATABASE_URL="postgresql://postgres:postgres@postgres:5432/fincontas?schema=public"

# Clerk authentication keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_ZGlzY3JldGUtYW50LTg4LmNsZXJrLmFjY291bnRzLmRldiQ"
CLERK_SECRET_KEY="sk_test_xxx"
# Legacy Vite environment variable, keep if still using the Vite front-end build
# (o build Vite reaproveita automaticamente NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY quando definido)
VITE_CLERK_PUBLISHABLE_KEY=""
# Opcional: o front-end detecta automaticamente os domínios públicos e roteia /api via Worker
VITE_API_BASE_URL=""
